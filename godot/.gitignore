# Godot 4 (GDScript)
.godot/
.import/

# Export output (CI produces this). export_presets.cfg IS committed on purpose
# so the Web preset ships to CI — do not ignore it.
build/
export.cfg

# OS / editor
.DS_Store
